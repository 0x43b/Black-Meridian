# COMMIT NOTES // V3.3

Recommended commit message:

```txt
Add Book I V3.3 Act I full prose expansion
```

Recommended repo placement if using Book 0 as root:

```txt
main-repo/
  index.html        # Book 0 / The Unsealed Archive
  assets/
  book-1/           # This V3.3 folder contents
    index.html
    style.css
    app.js
    story.js
    assets/
    README.md
    VERSION.txt
```

Use the wrapped ZIP if placing inside an existing Book 0 repo. Rename the extracted folder to `book-1`.

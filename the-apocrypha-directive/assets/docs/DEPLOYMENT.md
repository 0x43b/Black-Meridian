# Deployment

Drag the contents of this folder into your `the-apocrypha-directive/` folder, or rename this whole folder to `the-apocrypha-directive` and place it inside your Book 0 repo.

This V4 build is single-scroll reader mode. It keeps the archive gates and animations but removes the two-page spread as the main reading surface.

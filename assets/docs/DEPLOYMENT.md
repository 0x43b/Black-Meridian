# Deployment

Use `black-meridian-book0-v4-github-pages-root.zip` for your main repo root.

It should sit at:

`yourdomain.com/`

Book I should sit at:

`yourdomain.com/the-apocrypha-directive/`

The final Dead Drop button links to `the-apocrypha-directive/`.

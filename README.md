# BLACK MERIDIAN // Book 0 // V4 Night Archive Reader

This is the V4 upgrade for the main public Book 0 root site.

## Use

For the main repository root, use:

`black-meridian-book0-v4-github-pages-root.zip`

This should replace the root site files.

Book I remains in:

`the-apocrypha-directive/`

## What changed

- Single-scroll reader instead of two-page spread.
- V4 animated signal net, code rain, scanlines, noise, pulse effects.
- Longer Book 0 prose files.
- Creator-facing outline text removed.
- End Dead Drop links to `the-apocrypha-directive/`.


## V4.1 Asset Restore

V4.1 keeps the V4 single-scroll night reader and animation upgrade, but restores the Book 0 SVG image asset library from the prior V3 build.

This fixes the V4 packaging issue where the older Book 0 SVGs were not carried forward.


## V4.3 All-Chapter Clean Story Patch

Every chapter/node has been cleaned of repeated filler padding and reflowed into normal book-style paragraphs.
